# Make sure you change credentials.py to match your confluence credentials
# Change the id of the confluence space you want to crawl in confluence.py
# Crawler can be started with `scrapy crawl confluence`